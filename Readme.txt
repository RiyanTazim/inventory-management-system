Greetings.

Its a simple instruction note for the project Inventory Management System.
Turn on Apache & MySQL from Xampp. Copy .env.example file and change the name into .env file inside project files. Change the database name as inventory-management-system in that newly created .env file.
Import the database given with the project.
A user is created their which you can use to login or you may create a new one.

User Email: admin@gmail.com
Password: admin123

User Authetication exixt so you can't access admin dashboard without login.
There is no multi user authentication. So any logged in user can access the dashboard.

Run command "php artisan serve" to see the onboarding view of the project.

A simple user friendly UI is built to access the product management system. 
